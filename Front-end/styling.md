Styling
========

Refers to Twig & Sass or css for components. 

Strategy Using SDC

- In the folder, create "components" folder
- For each independent twig component, create a subfolder
- Create a corresponding_component.scss file
- Ins style.scss, import SASS component's
@import "../templates/component_component.scss"
